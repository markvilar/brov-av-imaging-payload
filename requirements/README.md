# Requirements

Payload requirements, interface specifications (ICDs), and the operating
envelope. These define *what the payload must do and withstand* — inputs to the
trade studies and design decisions, not the decisions themselves.
